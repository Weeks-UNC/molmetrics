name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - pip

  # Package dependencies
  - rdkit>=2025.03.2
  - pandas
  - numpy
  - openpyxl

  # Testing dependencies
  - pytest
  - pytest-cov
  - codecov
